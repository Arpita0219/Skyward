# TODO - UI animation & polish

- [x] Update `frontend/src/index.css` with reduced-motion + refined glass/animation helpers

- [x] Update `frontend/src/pages/Home.jsx` to add parent stagger and consistent section spacing

- [x] Update `frontend/src/components/HeroCard.jsx` for smoother entrance animation and tighter responsive spacing


- [x] Update `frontend/src/components/WeatherHighlights.jsx` + `HighlightCard.jsx` for staggered entrances and subtle hover

- [x] Update `frontend/src/components/HourlyForecast.jsx` for staggered card entrances


- [x] Update `frontend/src/components/WeeklyForecast.jsx` for entrance animation on rows and responsive padding

- [ ] Leave `AirQuality.jsx` / `Loading.jsx` untouched until verified
- [x] Run frontend `npm run dev` (or build) to ensure no compile errors



